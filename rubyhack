#!/usr/bin/env ruby

require 'bundler/setup'

require 'colorized_string'
require 'rgl/adjacency'
require 'rgl/dot'

module Tag
  GRAPH = 'Graph' # TODO Rename everything World instead of Graph?
  MONSTER = :monster
  PLAYER = :player
  WALL = :wall
end

require 'recs'

require 'require_all'
require_all './**/*.rb'

Game.new
