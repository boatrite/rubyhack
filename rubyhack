#!/usr/bin/env ruby

require 'bundler/setup'

require 'colorized_string'

module Tag
  NODE = 'Node'
  MONSTER = :monster
  PLAYER = :player
  WALL = :wall
end

require 'recs'

require 'require_all'
require_all './**/*.rb'

Game.new
